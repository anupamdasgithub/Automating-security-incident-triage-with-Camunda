# Local servers for the agentic variant.
#
# The mcp upper bound is not optional. SDK 2.0 renamed FastMCP to MCPServer with
# no alias, and `pip install mcp` now resolves to 2.x, so an unpinned install
# breaks soc_mcp_server.py at import time.

mcp>=1.28,<2
flask>=3.0

# kafka-python is used by incident_producer.py only.
kafka-python>=2.0
